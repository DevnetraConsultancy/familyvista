"use client";

/**
 * Crop was removed. This component is kept as a no-op placeholder so the
 * imports and wiring in the rest of the codebase stay consistent until the
 * crop references are fully removed.
 */
export function CropDialog() {
  return null;
}
